language: python
python:
    - "3.7"
install:
    - pip install -U tox-travis
    - pip install -e ."[all]"
    - pip install -e ."[test]"
script:
    - python setup.py test
after_script:
    - bash scripts/deploy.sh
after_success:
    - pip install coveralls
    - coveralls
